export type EventTargtProps = {
  target: {
    name: string,
    value: string;
  };
};

export type LoginType = {
  username: string;
  password: string;
};

export type RegisterType = {
  username?: any;
  password?: any;
  email?: any;
};
